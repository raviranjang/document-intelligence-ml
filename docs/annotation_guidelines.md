# Annotation guidelines

The initial annotation schema has not been defined. Before labeled extraction data is accepted,
this document must version and describe supported labels, entity boundaries, ambiguous cases,
multi-token values, OCR errors, missing fields, duplicate candidates, exclusions, and examples.

Changes to label semantics require a label-schema version change.
