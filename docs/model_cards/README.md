# Model cards

Every production candidate requires a model card documenting purpose, model family, inputs,
outputs, training and evaluation datasets, aggregate and slice metrics, limitations, unsupported
uses, ownership, and release identity. Do not add placeholder performance claims.
